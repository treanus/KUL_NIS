# `clinical_fmri_dmri/` — types 1-4

Intentionally empty of configs: `_base/` **is** the fmri/dmri set, so types 1-4
need no overrides. Put a file here only to change it for types 1-4 without
changing it for the DBS and DTI-ALPS types.
