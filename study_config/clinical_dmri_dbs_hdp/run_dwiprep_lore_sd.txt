# KUL_Neuro_Imaging_Tools
#  this is the config file for KUL_preproc_all
#  lore-sd variant: runs dhollander + lore-sd FOD estimation on upsampled (1.3mm) data
#  use with: KUL_clinical_fmridti.sh -D run_dwiprep_lore_sd.txt ...

# Here you specify which subjects to process
BIDS_participants: 

# dwiprep processing
#  dwiprep_options: space-separated list of FOD methods (dhollander, tax, tournier, lore_sd)
#  use_native_dwi: 1 = use native resolution data (dwi/biascorr.mif, default); lore_sd
#                      outputs are resampled to 1.3mm afterwards for downstream compatibility
#                  0 = use upsampled data (dwi_preproced.mif, 1.3mm iso)
do_dwiprep: 1
dwiprep_options: dhollander lore_sd
#  loresd_env: name of the conda env with lore_sd installed; defaults to $KUL_LORESD_ENV ('lore_sd', the fixed name the KUL_NIS installer creates it under) when left blank -- only set this if you used a different env name
loresd_env:
use_native_dwi: 1
synbzero_disco_instead_of_topup: 0
rev_phase_for_topup_only: 1
dwi2mask_method: 4
#  shard_recon: 1 to use shard-recon (SHARD reconstruction) instead of eddy for motion/distortion correction (see KUL_dwiprep.sh -c); default 0 (use eddy)
shard_recon: 0
eddy_options: --slm=linear --repol
dwiprep_ncpu: 40
dwiprep_simultaneous: 1

exit_after: 1
